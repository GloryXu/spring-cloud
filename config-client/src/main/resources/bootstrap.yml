spring:
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
      label: master
      username: user
      password: userPwd
      fail-fast: true
  application:
    name: itscore
server:
  port: 8887