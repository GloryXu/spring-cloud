spring:
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
      label: config-label-test
      username: user
      password: userPwd
  application:
    name: itscore
server:
  port: 8887